# DDAS Content Automation - Python Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.28.0              # HTTP client for APIs
python-dateutil>=2.8.2        # Date/time utilities
pydantic>=1.9.0               # Data validation

# Hive blockchain interaction
beespy>=0.2.0                 # Hive blockchain SDK (alternative: hive-python)

# Configuration and environment
python-dotenv>=0.20.0         # Load .env files
pyyaml>=6.0                   # YAML file support

# API clients (install based on provider choice)
# Uncomment the provider you'll use:

anthropic>=0.3.0            # Claude API
openai>=0.27.0              # OpenAI API
ollama>=0.1.0               # Ollama local LLM (if using local)

# Optional: async support
aiohttp>=3.8.0                # Async HTTP client
asyncio-contextmanager>=1.0   # Better async context management

# Optional: database and caching
# sqlalchemy>=2.0              # Database ORM (if using DB)
# redis>=4.5.0                 # Redis client for caching

# Optional: image generation
# stability-sdk>=0.3.0         # Stable Diffusion API

# Development dependencies (optional)
pytest>=7.0                   # Testing framework
pytest-asyncio>=0.20.0        # Async test support
black>=22.0                   # Code formatting
flake8>=4.0                   # Linting
mypy>=0.990                   # Type checking

# Logging and monitoring
colorama>=0.4.6               # Colored console output
