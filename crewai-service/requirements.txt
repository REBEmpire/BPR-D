# BPR&D CrewAI Service Dependencies
# Python >=3.10, <3.14

# Core CrewAI framework (independent of LangChain as of 2026)
crewai[tools]

# LLM Provider extras
# Anthropic (Claude) - installed via crewai[anthropic] or directly
anthropic

# Google Gemini - installed via crewai[google-genai] or directly
google-generativeai

# xAI Grok uses OpenAI-compatible API (no extra package needed)
# Abacus.AI uses OpenAI-compatible API (no extra package needed)

# FastAPI server
fastapi
uvicorn[standard]

# Utilities
requests
python-dotenv
pyyaml
crewai[google-genai]
